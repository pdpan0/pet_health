import 'package:flutter/material.dart';

class PetFormWidget extends StatelessWidget {
  const PetFormWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pet Form Widget'));
  }
}
